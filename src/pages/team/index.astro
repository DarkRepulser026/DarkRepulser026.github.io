---
import AuthorCard from '@/components/author-card.astro'
import Breadcrumbs from '@/components/breadcrumbs.astro'
import PageHead from '@/components/page-head.astro'
import Layout from '@/layouts/layout.astro'
import { getAllAuthors } from '@/lib/data-utils'

const authors = await getAllAuthors()
---

<Layout class="max-w-5xl">
  <PageHead slot="head" title="Team" />

  <Breadcrumbs items={[{ label: 'Team', icon: 'lucide:users' }]} />

  <section class="team-shell">
    <header class="team-header prose not-prose">
      <h1 class="team-title">People behind my projects and builds.</h1>
      <p class="team-updated">Last updated: 2026-04-15</p>
    </header>

    <section class="team-list-wrap">
      {
        authors.length > 0 ? (
          <ul class="team-list">
            {authors.map((author, index) => (
              <li class="reveal" style={`--delay: ${120 + index * 80}ms;`}>
                <AuthorCard author={author} />
              </li>
            ))}
          </ul>
        ) : (
          <p class="team-empty">[ NO TEAM FOUND ]</p>
        )
      }
    </section>
  </section>
</Layout>

<style>
  .team-shell {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);
    width: 100%;
    max-width: 100%;
    overflow-x: clip;
    margin: 0;
    padding-left: 0;
    padding-right: 0;
    padding-top: var(--space-md);
    padding-bottom: var(--space-3xl);
  }

  .team-header {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .team-title {
    font-size: clamp(2rem, 4.5vw, 3.2rem);
    line-height: 1.05;
    letter-spacing: -0.03em;
    color: var(--text-display);
  }

  .team-updated {
    color: var(--text-secondary);
    font-family: 'Space Mono', monospace;
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .team-list-wrap {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    width: 100%;
    max-width: 100%;
    margin: 0;
    padding: 0;
  }

  .team-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    width: 100%;
    max-width: 100%;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .team-list > li {
    display: block;
    width: 100%;
    max-width: 100%;
    margin: 0;
    padding: 0;
  }

  .reveal {
    opacity: 0;
    transform: translateY(12px);
    animation: reveal 420ms cubic-bezier(0.25, 0.1, 0.25, 1) forwards;
    animation-delay: var(--delay, 0ms);
  }

  .team-empty {
    font-family: 'Space Mono', monospace;
    color: var(--text-secondary);
    letter-spacing: 0.08em;
  }

  @keyframes reveal {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>